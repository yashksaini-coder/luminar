services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
    environment:
      - LUMINAR_NODE_COUNT=20
      - LUMINAR_LOG_LEVEL=INFO
    restart: unless-stopped
