services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - LUMINAR_NODE_COUNT=20
      - LUMINAR_LOG_LEVEL=INFO
    restart: unless-stopped
